/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'standalone',
    // Enable if you need to use environment variables at build time
    // env: {
    //   NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL,
    // },
  }
  
  module.exports = nextConfig